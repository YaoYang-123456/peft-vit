# 在 GPU 上验证(PyTorch 2.x / CUDA 11.8+)。版本以你的环境为准,可用以下命令查看:
#   python -c "import torch,timm;print('torch',torch.__version__,'timm',timm.__version__)"
torch>=2.1
torchvision>=0.16
timm>=1.0.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10            # 配对 t 检验 (scripts/summarize_results.py)
